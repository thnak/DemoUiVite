// ----------------------------------------------------------------------
// Standard table configuration constants
// ----------------------------------------------------------------------

/**
 * Standard rows per page options for all master data list pages
 * Use this constant to ensure consistency across all table paginations
 */
export const STANDARD_ROWS_PER_PAGE_OPTIONS = [5, 10, 25, 50] as const;

/**
 * Default rows per page for master data lists
 */
export const DEFAULT_ROWS_PER_PAGE = 10;
